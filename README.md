# rust-web-server-yew

use yew and bulma.

## install

```shell
# rust wasm
rustup target add wasm32-unknown-unknown
# wasm cli
cargo install trunk wasm-bindgen-cli
# run dev
trunk serve
# build release
trunk build --release
```
